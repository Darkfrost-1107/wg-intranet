version: '3.8'

services:
  postgres:
    image: postgres
    container_name: my_postgres
    restart: always
    environment:
      POSTGRES_USER: Ginger_Ale
      POSTGRES_PASSWORD: Ginpachi_110705
      POSTGRES_DB: monitor_db
    ports:
      - "5432:5432"
    volumes:
      - new_postgres_data:/var/lib/postgresql/data

volumes:
  new_postgres_data:
